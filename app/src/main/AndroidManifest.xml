<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.manzolik.gmanzoli.mytrains">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <receiver android:name=".SchedulingAlarmReceiver" />
        <service android:name=".TrainStatusSchedulingService" />

        <activity
            android:name=".TrainStatusActivity"
            android:configChanges="orientation|screenSize">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".AddReminderActivity"
            android:label="@string/add_reminder_title"
            android:parentActivityName=".TrainStatusActivity" />
        <activity
            android:name=".ManageReminderActivity"
            android:label="@string/manage_reminder_title"
            android:parentActivityName=".TrainStatusActivity" />
        <activity
            android:name=".SettingsActivity"
            android:label="@string/settings_title"
            android:parentActivityName=".TrainStatusActivity"/>
    </application>

</manifest>
